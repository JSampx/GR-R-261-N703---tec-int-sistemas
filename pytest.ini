[tool:pytest]
# Configuração do pytest
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = auto

# Timeout padrão para testes
timeout = 10

# Verbose output
addopts = -v --tb=short
